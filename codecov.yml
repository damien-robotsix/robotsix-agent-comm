coverage:
  status:
    project:
      default:
        target: 85%
        threshold: 2%
    patch:
      default:
        target: 85%
        threshold: 2%

comment:
  layout: "header, diff, flags, files"
  behavior: default
  require_changes: false
