# Examples

This directory holds runnable examples demonstrating how to use
`robotsix-agent-comm`.

It is currently empty: examples arrive once the messaging protocol and
transport layer land in later phases. This file documents the directory's
purpose and keeps it tracked by git in the meantime.
