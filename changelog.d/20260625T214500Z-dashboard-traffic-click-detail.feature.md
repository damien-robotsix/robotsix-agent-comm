Broker dashboard traffic rows are now clickable to expand a detail panel showing the record's full metadata — message id, correlation id, source, destination, topic, type, disposition, status, exact body size, and the precise (ISO + epoch) timestamp. The expanded state is keyed by message id so it survives the 4-second auto-refresh. (Message *bodies* are not retained by the broker, so only routing metadata is shown.)
